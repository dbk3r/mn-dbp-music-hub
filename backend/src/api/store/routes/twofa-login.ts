
// Disabled duplicate express router; store auth is handled by
// `src/api/store/auth/init/route.ts` (framework route). Keeping this
// file empty to avoid double-registration of the same path.

export default {}