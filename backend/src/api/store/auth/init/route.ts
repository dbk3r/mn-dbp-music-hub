import type { MedusaRequest, MedusaResponse } from "@medusajs/framework/http"

// This file intentionally left blank — route handled by express router
// at `src/api/store/routes/twofa-login.ts`. Deleting file at runtime
// can be disruptive during local development; keep file absent of logic.
export {};
